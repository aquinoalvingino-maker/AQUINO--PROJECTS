# This folder is no longer used for dataset storage

As of the database migration, uploaded datasets, upload history, and
cleaning reports are stored in MySQL (see `../schema.sql` and
`../XAMPP_SETUP.md`) instead of flat files here.

This folder is kept only as a harmless scratch space / placeholder so
the project structure doesn't break if something expects it to exist.
